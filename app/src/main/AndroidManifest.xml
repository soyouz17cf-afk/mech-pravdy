<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- РАЗРЕШЕНИЯ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Для камеры (кнопка СМОТРЕТЬ) -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Для доступа к файлам .gguf -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        tools:ignore="ScopedStorage" />
    
    <!-- Для Android 11+ (доступ ко всем файлам) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    
    <!-- Для голосового ввода -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- Для уведомлений Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Mech Pravdy"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Кастомные вьюхи (чтобы не было ошибок) -->
        <activity
            android:name="com.mechpravdy.neo.MatrixHeaderView"
            android:exported="false" />
        <activity
            android:name="com.mechpravdy.neo.MatrixChatBackground"
            android:exported="false" />

    </application>
</manifest>
